import expect from 'expect.js';

import * as ConsoleLogger from '../../src/metrics/ConsoleLogger';

describe('ConsoleLogger', () => {
	describe('log', () => {
		it('should log the given message to the console');
	});
});
